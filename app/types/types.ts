export interface passwordShowType {
    signup: boolean,
    signupver: boolean,
    login: boolean
}

export interface loginType {
    username: string,
    password: string
}

export interface signupType {
    username: string,
    email: string,
    password: string,
    passwordver: string
}

